[tool.poetry]
name = "medrisk-features"
version = "0.2.0"
description = "Professional medical, metabolic and lifestyle feature engineering for ML risk models"
authors = ["Rostand Surel <s239150.eps@gmail.com>"]
readme = "README.md"
license = "MIT"
homepage = "https://github.com/Manda404/medrisk-features"
repository = "https://github.com/Manda404/medrisk-features"
keywords = [
    "healthcare",
    "feature-engineering",
    "machine-learning",
    "diabetes",
    "clinical-ml",
    "mlops",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Healthcare Industry",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
]
packages = [{ include = "medrisk_features", from = "src" }]

[tool.poetry.dependencies]
python = "^3.9"
pandas = "^2.0"
numpy = "^1.24"
loguru = "^0.7.3"
# Optional extras — install only what you need
mlflow      = { version = ">=2.0",  optional = true }
xgboost     = { version = ">=1.7",  optional = true }
catboost    = { version = ">=1.2",  optional = true }
lightgbm    = { version = ">=4.0",  optional = true }
scikit-learn = { version = ">=1.3", optional = true }

[tool.poetry.extras]
# pip install medrisk-features[mlflow]
mlflow = ["mlflow"]
# pip install medrisk-features[xgboost]
xgboost = ["xgboost"]
# pip install medrisk-features[catboost]
catboost = ["catboost"]
# pip install medrisk-features[lightgbm]
lightgbm = ["lightgbm"]
# pip install medrisk-features[boosting]  → all 3 boosting libs + mlflow
boosting = ["mlflow", "xgboost", "catboost", "lightgbm", "scikit-learn"]
# pip install medrisk-features[all]  → everything
all = ["mlflow", "xgboost", "catboost", "lightgbm", "scikit-learn"]

[tool.poetry.group.dev.dependencies]
pytest = "^7.4"
pytest-cov = "^4.1"
ruff = "^0.4"
black = "^24.0"
mypy = "^1.10"
pandas-stubs = "^2.0"
mlflow = ">=2.0"

# ---------------------------------------------------------------------------
# Ruff linter configuration
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]
ignore = ["E501"]  # line-length handled by black

# ---------------------------------------------------------------------------
# Black formatter configuration
# ---------------------------------------------------------------------------
[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311"]

# ---------------------------------------------------------------------------
# Mypy type checking configuration
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
ignore_missing_imports = true

# ---------------------------------------------------------------------------
# Pytest configuration
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
